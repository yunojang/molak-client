import { render, screen } from '@/lib/test-lib';
import { {{ pascalCase name }} } from '{{directory}}{{pascalCase name}}';

describe('{{ pascalCase name }} Component test suite', () => {
  test('render inital text', async () => {
    // render
    render(<{{pascalCase name}} />);

    // query
    const head = screen.getByRole('heading');

    // assertion
    expect(head).toHaveTextContent('{{ pascalCase name }}');
  });
});
