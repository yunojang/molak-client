import { FC } from 'react';

interface MolakCarouselProps {
  _?: any;
}

const MolakCarousel: FC<MolakCarouselProps> = () => {
  // get landing data on hooks

  // set component to carousel

  return <></>;
};

export default MolakCarousel;
